- name: play level variables
  hosts: local
  connection: local
  vars_files:
  - filevars.yaml
  
  tasks:
  - name: print variables
    vars:
      COURSE: "ANSIBLE"
    ansible.builtin.debug:
      msg: "Course is {{ COURSE }}, Trainer is {{ TRAINER }} and duration is {{ DURATIOIN }}"